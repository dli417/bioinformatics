# Make a matrix of 80 random numbers drawn from a normal distribution (see the
# function rnorm()). You can decide on the values for mean and sd, and the
# dimensions of the matrix.

matrix(rnorm(80), ncol = 4)
